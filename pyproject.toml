[project]
name = "found-integration-testing"
version = "0.1.0"
description = "FOUND integration tests and batch runner"
requires-python = ">=3.14"
dependencies = [
    "ipykernel>=6.31.0",
    "pandas",
    "matplotlib",
    "limb @ git+https://github.com/UWCubeSat/found-tools.git@satellite-pos-orientation",
]

[dependency-groups]
dev = []

