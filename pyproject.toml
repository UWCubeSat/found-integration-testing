[project]
name = "found-integration-testing"
version = "0.1.0"
description = "FOUND integration tests and batch runner"
requires-python = ">=3.9"
dependencies = [
    "ipykernel>=6.31.0",
    "pandas",
    "matplotlib",
]

[tool.uv]
dev-dependencies = []
